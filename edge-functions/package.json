{
  "name": "nav-site-edge-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "EdgeOne Makers 项目内 Edge Functions（edge-functions/api/[[default]].js）：登录（数据库侧 pgcrypto.crypt 校验 + HS256 签发 JWT）与后台写操作（nav_admin 执行 SQL）。本文件为函数依赖唯一声明来源，Makers 构建/部署时据此安装；环境变量：DATABASE_URL_ADMIN、JWT_SECRET（Makers 控制台配置，函数经 context.env 读取，不进前端 bundle）",
  "main": "api/[[default]].js",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "jose": "^6.2.10"
  }
}