{
  "name": "nav-site-edge-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "EdgeOne Functions 薄代理：登录（bcrypt 校验 + HS256 签发 JWT）与后台写操作（nav_admin 执行 SQL）。环境变量：DATABASE_URL_ADMIN、JWT_SECRET（仅函数服务端配置，不进前端 bundle）",
  "main": "index.js",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.10"
  }
}